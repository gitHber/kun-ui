# @kun-ui/styled

kun-ui React styled

## Installation

Install the package in your project directory with:

```sh
// with npm
npm install @kun-ui/styled

// with yarn
yarn add @kun-ui/styled
```

## Documentation

[The documentation]()
